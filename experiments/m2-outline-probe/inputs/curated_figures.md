# Curated figures — ibd_phage_targeting / talk-30

(No figures shortlisted for this probe.)

The M2-lite outline experiment deliberately runs with an empty figure
shortlist. `slide_compose.v1.md`'s escape hatch covers this: compose
without `data_figure` slides, substituting `claim_evidence` (without a
figure) and `methods_summary`. This keeps the probe focused on the
cross-section coordination of text content — transitions, register, and
arc — and keeps per-call cost low. Figure-scope coordination is part of
M2-lite but is the least-uncertain part; it is not exercised here.
