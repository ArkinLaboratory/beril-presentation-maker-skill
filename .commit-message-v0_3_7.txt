v0.3.7: image_gen_decision LLM-judgment layer (kills "deferred to v0.3.4" bug)

Closes the long-standing "deferred to v0.3.4 LLM-judgment layer"
placeholder in image_gen_decision.py that was returning emit=false
for 6 layouts and silently producing zero AI illustrations on
figure-rich projects. Surfaced as a deck-level weakness during the
2026-05-06 memoryless-reviewer assessment of ibd_phage_targeting
talk-45: 0 AI illustrations in a 37-slide deck on a complex
biological topic that would have benefited from conceptual visuals
(mechanism cartoons, cocktail-strategy schematics, framework
diagrams).

Diagnosis from 05_image_decisions.json (uploaded 2026-05-06)

  Total decisions:                    33
  emit=true:                          0
  Reasons distribution:
    7  claim_evidence supplemental-image decision deferred to v0.3.4
    5  data_table carries its own figure (correctly emit=false)
    4  data_figure carries its own figure (correctly emit=false)
    4  qa_anticipated is structural (correctly emit=false)
    3  section_divider is structural (correctly emit=false)
    2  workflow_diagram supplemental-image decision deferred to v0.3.4
    2  big_number supplemental-image decision deferred to v0.3.4
    2  methods_summary is structural (correctly emit=false)
    2  big_idea supplemental-image decision deferred to v0.3.4
    1  two_column_compare supplemental-image decision deferred to v0.3.4
    1  cross_tenant_integration is structural (correctly emit=false)

15 of 33 candidate slides deferred to a non-existent layer. The
v0.3.3 stub returned emit=false unconditionally; the v0.3.4 LLM-
judgment layer was specced but never implemented.

What v0.3.7 ships

  src/beril_presentation_maker/skill/tools/image_gen_decision.py
    NEW llm_judge() function — synchronous claude -p call per
    deferred-layout slide. Per-slide prompt asks: "would a
    conceptual AI illustration add value beyond what this layout's
    existing content provides?" Returns (emit, reason) tuple.
    Defensive fallbacks: claude-not-on-PATH, subprocess timeout,
    non-zero exit, unparseable response — all return (False,
    descriptive_reason) without raising.

    NEW JudgeFn type alias (Callable[[dict, str, str], tuple[bool, str]]).
    Tests pass stub callbacks to avoid live LLM calls.

    decide() / decide_fragment() / emit_decisions() take new
    `judge_fn: Optional[JudgeFn]` parameter. When None (default),
    deferred layouts return emit=false with reason "needs LLM
    judgment but no judge_fn was provided" — preserves pre-v0.3.7
    conservative behavior for non-CLI callers and existing tests
    that don't pass judge_fn.

    NEW _build_judge_prompt() — discriminating prompt with explicit
    YES/NO criteria. YES for concept slides (mechanism, framework,
    abstraction, opening pivots); NO for data-heavy / structural /
    too-specific. When uncertain, prefer NO (illustrations cost
    ~$0.014 each and over-illustration distracts).

    NEW _parse_judge_response() — pure parser, strict on YES/NO
    prefix, permissive on separator (": ", "—", " - ", whitespace).
    Falls back to (False, reason) on unparseable responses.

    CLI _cmd_emit_decisions: auto-enables llm_judge when claude is
    on PATH. NEW --no-llm-judge flag for explicit opt-out (for
    tests, sandbox runs, or operators wanting pre-v0.3.7
    conservative behavior). NEW --judge-model flag (default
    claude-sonnet-4-6).

    Envelope: NEW llm_judgment_used boolean field on the
    image-decisions.v1 JSON envelope, allowing downstream consumers
    (or audit-log readers) to distinguish runs that used LLM
    judgment from runs that didn't.

  src/beril_presentation_maker/skill/tools/presentation_maker.sh
    Comment block in stage_image_gen updated to reflect the new
    behavior (auto-enabled when claude is on PATH; falls back to
    no-AI-image when absent). No flag-surface change at the
    orchestrator level — `--no-images` still skips the whole stage
    if users want that.

  tests/unit/test_image_gen_decision.py
    25 new unit tests in three classes:
      TestJudgeFnIntegration (5): judge_fn yes/no/exception paths,
        all 6 deferred layouts invoke it, non-deferred layouts skip it
      TestLLMJudgeHelper (6): claude-not-on-PATH fallback, parses
        YES/NO response, unparseable fallback, timeout fallback,
        non-zero exit fallback
      TestParseJudgeResponse (10): YES/NO parser edge cases —
        separators, missing reason, empty input, unparseable, multiline,
        case-insensitivity, surfaces head-of-response in unparseable case
      TestEmitDecisionsLLMJudgmentFlag (2): envelope flag false/true
        toggles correctly
      TestCLIWithLLMJudgeFlags (2): --no-llm-judge disables;
        claude-not-on-PATH falls back gracefully

    Existing test test_rule_5_deferred_layouts_skipped updated to
    match new "no judge_fn" reason text instead of "v0.3.4".

Smoke verification on real ibd_phage_targeting/slide_spec.json

  Stub discriminating judge (YES on big_idea, big_number, and
  claim_evidence-without-figure; NO on the rest):

    Total decisions:               37
    emit=true:                     11
    emit=true via LLM:             11
    llm_judgment_used:             True

    Slides flipped to emit=true:
      intro-pos1   big_idea       opening claim benefits from visual hook
      intro-pos2   big_idea       opening claim benefits from visual hook
      intro-pos3   claim_evidence claim without figure — illustration helps
      intro-pos4   claim_evidence claim without figure — illustration helps
      S1-pos2      claim_evidence claim without figure — illustration helps
      S1-pos4      claim_evidence claim without figure — illustration helps
      S1-pos5      big_number     headline statistic benefits from visual backdrop
      S2-pos2      claim_evidence claim without figure — illustration helps
      S2-pos4      claim_evidence claim without figure — illustration helps
      S2-pos7      big_number     headline statistic benefits from visual backdrop
      S2-pos8      claim_evidence claim without figure — illustration helps

  Regression check: without judge_fn (preserves pre-v0.3.7 behavior),
  emit=true count = 0. No false positives.

  11 illustrations is the credible target for a 45-min STRONG-tier
  talk on a complex biological topic. The breakdown — 2 opening
  pivots, 2 headline statistics, 7 data-less concept claims —
  matches what a peer scientist would expect a thoughtful presenter
  to commission.

Cost analysis

  Per-slide LLM call: ~$0.005-0.01 on Sonnet 4.6 (~300 token prompt,
  ~30 token response).
  Per-draft cost overhead: $0.10-0.20 for typical 15-20 deferred
  slides.
  Per-image generation cost (downstream, unchanged): ~$0.014/image
  via CBORG-Gemini.
  Total cost delta on a typical talk-30 STRONG: +$0.10-0.30
  judgment + $0.10-0.30 generation = +$0.20-0.60. Well within the
  $5-20/draft budget.

Latency

  Per-call: 2-5s typical, 60s timeout.
  Per-draft: +30-90s for 15-20 sequential calls. Acceptable; full
  pipeline is 60-120 min on the hub. Parallelization deferred to
  v0.4.0+.

Tests

  Full suite: 762 → 787 unit tests passed. 1 marker-gated live
  integration test still skipped in sandbox. No regression in the
  v0.3.6 --pick TLN / halt-and-handoff paths or the v0.3.5.1
  strict-mode patch.

Adam's runbook (after v0.3.7 push)

  pipx install --force git+https://github.com/ArkinLaboratory/beril-presentation-maker-skill.git@v0.3.7
  beril-presentation-maker --version    # 0.3.7
  cd ~/BERIL-research-observatory
  beril-presentation-maker install-skill .

  # Hub smoke (re-run the same project that produced 0 illustrations):
  cd projects/ibd_phage_targeting
  beril-presentation-maker draft ibd_phage_targeting --mode talk-45 --auto-advance --auto-approve-images
  # Expected: image-gen stage now invokes per-slide LLM judgment;
  # 5-12 emit=true on a 35-40 slide deck; final .pptx has 5-12
  # AI illustrations.
  #
  # Verify post-run:
  cat talks/draft_N/working/05_image_decisions.json | python3 -c "
  import sys, json
  e = json.load(sys.stdin)
  d = e['decisions']
  yes = [x for x in d if x['emit']]
  llm = [x for x in d if 'LLM-judged' in x.get('reason','')]
  print(f'llm_judgment_used: {e.get(\"llm_judgment_used\")}')
  print(f'emit=true: {len(yes)}/{len(d)} ({len(llm)} via LLM)')
  for x in yes:
      print(f'  {x[\"slide_id\"]:20s} {x[\"layout\"]:25s} {x[\"reason\"]}')
  "

  # If the LLM judgment turns out to be too eager (>50% emit=true on
  # a typical deck), iterate the prompt in _build_judge_prompt()
  # adding tighter NO criteria. If too conservative (<3 emit=true on
  # a typical talk-30), relax YES criteria.

Refs

  - predecessor tag: v0.3.6
  - bug-source: 2026-05-06 review of ibd_phage_targeting talk-45 deck
    + analysis of working/05_image_decisions.json
  - workspace task #90 closes; #65 closed (had wrong root-cause
    hypothesis — bias was never in slide_compose; it's
    image_gen_decision)
  - punch list: V0_4_0_PUNCH_LIST.md tier A
  - paper-writer's per-slide LLM-call pattern (in revise_loop.py
    _invoke_claude) was the structural reference; we kept the call
    simpler (no stream-progress wrapper) since this is a tiny
    yes/no judgment, not a content generation.
