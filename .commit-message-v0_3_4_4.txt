v0.3.4.4: docs cleanup + reviewer-pass fixes pre-hub-install

Final pre-deployment cleanup release. Two parts:

PART 1 — README + RELEASE_NOTES rollup

- README rewritten from stale v0.1-spec-only state to reflect
  v0.3.4.4 reality. Status line, install commands, 14-stage
  pipeline diagram, 16-layout vocabulary, 4-zone output tree (with
  v0.3.4.2 audit/stage-metadata + audit/runs), full flag set in
  the usage block, BERIL workflow integration diagram, status
  caveats. Replaces the v0.1 spec-only narrative that was the
  GitHub landing page until now.
- RELEASE_NOTES.md prepended with v0.3.3 → v0.3.4.4 entries
  (7 missing releases rolled up from .commit-message-*.txt
  contents). Now reads as a continuous changelog.
- HUB_INSTALL.md install URL pinned to v0.3.4.4. Misleading
  historical note about adversarial v3 corrected (v3 was first
  consumed in v0.3.3.1, not v0.3.4).
- SKILL.md status line updated to v0.3.4.4. Pitfall-detection
  section rewritten — was referencing non-existent
  `.claude/skills/pitfall-capture/` and `docs/pitfalls.md`.
  Replaced with a real recovery protocol pointing at
  audit/stage-logs/, audit/runs/run-N/summary.json, and
  HUB_INSTALL.md troubleshooting recipes. The pitfall-capture
  skill (if installed separately) still supersedes when present.

PART 2 — reviewer-pass fixes

Four parallel reviewer agents spotted the following pre-ship gaps:

a. **Stage-count drift across code+docs.** The bash orchestrator
   header (presentation_maker.sh L5-22) declared "11-stage flow"
   while the actual orchestrator runs 14 stages (image_gen +
   adversarial_review + revise_slides added across v0.3.0 and
   v0.3.3). draft.py docstring + parser description had the same
   stale "11-stage" wording. state.py docstring same. All four
   sources updated to "14-stage" with the full stage list
   including image_gen + adversarial_review + revise_slides.

b. **README usage block missing flags.** Reviewer found
   `--audience peer` accepted by draft.py but not surfaced in the
   README usage block. Added.

c. **HUB_INSTALL.md placeholder ambiguity.** L194 had
   `@v0.3.x.y` as a literal placeholder; replaced with concrete
   `@v0.3.4.4` plus comment clarifying any later v0.3.x.y tag is
   acceptable for upgrades.

d. **README + status-caveat block version drift.** Status line and
   v0.3.4.x feature mentions normalized to v0.3.4.4 / v0.3.4.x
   where appropriate (was inconsistent v0.3.4.3 references after
   v0.3.4.4 staging).

e. **SPEC.md + LAYOUT.md status banners.** Both still claimed
   "v0.1 specification — implementation has not started" /
   "implementation pending sign-off." Updated with banners
   pointing readers at README/SKILL.md/CONTRACT.md/RELEASE_NOTES
   for the current production state, while keeping the v0.1
   design-rationale narrative intact (it's still the load-bearing
   "why these choices" doc).

Reviewer findings deferred to later releases (P2):

- Rename `reference/master-template-source/KBase 2026 and beyond.potx`
  (filename has spaces; violates project rule). Defer to v0.3.4.5
  or v0.4 cleanup.
- Mark V0_2_0_PUNCH_LIST.md / V0_3_1_PUNCH_LIST.md /
  V0_3_3_ARCHITECTURE.md as HISTORICAL or move to archive/.
  Defer.
- Archive 21 .commit-message-v*.txt files at repo root. Defer.
- LAYOUT.md §2 prompt count update + §3 CLI subcommand list update.
  Defer to v0.4 docs cycle.
- DECISIONS.md walk-and-extend pass. Defer.

Reviewer findings reviewed and DISCARDED (false alarms):

- "revise_loop slide_id type confusion" — agent confused
  adversarial-review slide_id (int, slide.id field) with
  image-gen slide_id (string "S2-pos4" pattern). Two different
  contexts; revise_loop only sees the int form. No fix.
- "image_gen_decision partition silent drift" — already covered
  by `_validate_partition()` import-time assertion + the
  `test_all_layouts_have_a_verdict` test. No fix.
- "CONCEPT_STYLES no automated check" — already covered by
  `test_concept_styles_match_ai_image_prompt`. No fix.

Tests: 726/726 pass + 1 marker-gated live integration test
skipped (requires beril-adversarial install). Bash syntax clean.

Refs

- predecessor tag: v0.3.4.3
- task #70 closes
- 4 parallel reviewer audits 2026-05-04: code review, architecture
  docs review, user/tutorial docs review, repo cleanliness audit.
  Findings folded into Part 2.
- next: hub install validation (manual operator step using
  HUB_INSTALL.md), then task #50 multi-project smoke.
